name: Release App
on:
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-20.04
    environment:
      name: Release
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Release
        run: echo "Release!"