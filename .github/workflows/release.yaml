name: Release App
on:
  workflow_dispatch:

jobs:
  confirm:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Show Release
        run: echo ${{ secrets.MY_SECRET }}

  release1:
    runs-on: ubuntu-20.04
    needs: [confirm]
    environment:
      name: Release
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Release1
        run: echo ${{ env.MY_EMV }}

  release2:
    runs-on: ubuntu-20.04
    needs: [confirm]
    environment:
      name: Release
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Release2
        run: echo ${{ env.MY_EMV }}
